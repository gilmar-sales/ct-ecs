#ifndef SPACE_ECS_PLAYER_COMPONENT_HPP
#define SPACE_ECS_PLAYER_COMPONENT_HPP

namespace ecs {

    struct PlayerComponent {
        unsigned lifes;
        unsigned score;
    };

} // namespace ecs

#endif // SPACE_ECS_PLAYER_COMPONENT_HPP
